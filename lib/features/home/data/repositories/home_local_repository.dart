import '../datasources/home_local_datasource.dart';

/// Offline home logic. Add models under data/models/ when implementing home cache.
class HomeLocalRepository {
  const HomeLocalRepository({required HomeLocalDataSource localDataSource});
}
